#include <stdio.h>

/*
INSTRUCCIONES:
Completa la condición:
- Positivo si > 0
- En otro caso imprimir "Negativo o cero"
*/

int main() {
    int num;
    printf("Dime un numero"); 
       scanf("%d", &num);

    if (num > 0) {
        printf("Positivo\n");
    } else {
        printf("Negativo o cero\n");
    }
printf("arriba las chivas"); 
   return 0;
}
